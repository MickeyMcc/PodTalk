# Podstar
MVP project built off of Patrick Daly's 
'Simple Boilerplate for React Project'

## To start the app

`npm install -g webpack`

`npm install`

`npm run react-dev`

`npm run server-dev`
# podstar
